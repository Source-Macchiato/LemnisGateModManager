<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="LemnisGateLauncher.ModsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LemnisGateLauncher"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Mod Manager">

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="Base"/>
    </Window.SystemBackdrop>

    <Grid>
        <Grid.Resources>
            <Thickness x:Key="NavigationViewContentMargin">0,48,0,0</Thickness>
        </Grid.Resources>
        
        <Grid
        x:Name="AppTitleBar"
        Height="48"
        Margin="48,0,0,0"
        VerticalAlignment="Top"
        Padding="0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Image
            x:Name="AppFontIcon"
            Width="16"
            Height="16"
            HorizontalAlignment="Left"
            VerticalAlignment="Center"
            Source="/Assets/Square44x44Logo.png" />

            <TextBlock
            Margin="12,0,0,0"
            Grid.Column="1"
            VerticalAlignment="Center"
            Style="{StaticResource CaptionTextBlockStyle}"
            Text="Mod Manager" />
        </Grid>

        <NavigationView
        x:Name="NavigationViewControl"
        IsBackButtonVisible="Auto"
        IsBackEnabled="False"
        PaneDisplayMode="Left"
        IsTitleBarAutoPaddingEnabled="False"
        BackRequested="NavigationViewControl_BackRequested"
        ItemInvoked="NavigationViewControl_ItemInvoked">

            <NavigationView.HeaderTemplate>
                <DataTemplate>
                    <Grid Margin="0,-10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>

                        <Grid
                        x:Name="pageTitle"
                        Background="Transparent"
                        Height="44"
                        VerticalAlignment="Top">

                            <TextBlock
                        x:Name="title"
                        Style="{StaticResource TitleTextBlockStyle}"
                        FontSize="24"
                        VerticalAlignment="Center"
                        Text="{Binding}" />
                        </Grid>
                    </Grid>
                </DataTemplate>
            </NavigationView.HeaderTemplate>

            <NavigationView.AutoSuggestBox>
                <AutoSuggestBox
                    x:Name="controlsSearchBox"
                    MinWidth="200"
                    VerticalAlignment="Center"
                    x:FieldModifier="public"
                    KeyboardAcceleratorPlacementMode="Hidden"
                    PlaceholderText="Search"
                    QueryIcon="Find"
                    QuerySubmitted="OnControlsSearchBoxQuerySubmitted"
                    TextChanged="OnControlsSearchBoxTextChanged" />
            </NavigationView.AutoSuggestBox>

            <NavigationView.MenuItems>
                <NavigationViewItem Content="Home" Icon="Home" Tag="LemnisGateLauncher.Views.ModsHomePage" />
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame" Navigated="ContentFrame_Navigated" Padding="36, 10, 0, 0" />
        </NavigationView>
    </Grid>
</Window>
